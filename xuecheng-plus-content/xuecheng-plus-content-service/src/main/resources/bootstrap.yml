#微服务配置
spring:
  application:
    name: content-service #服务名 nacos配置中心 Data ID = 服务名-环境名（profiles.active).yaml
  cloud:
    #nacos配置
    nacos:
      server-addr: 192.168.101.65:8848
      #配置中心
      config:
        namespace: dev
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
  profiles:
    active: dev #环境名
